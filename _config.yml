# サイト全体の設定
lang: en

# 独自レイアウトを使うため minima テーマは指定しない

# すべてのページに layout: default を自動適用
defaults:
  - scope:
      path: ""
    values:
      layout: default

include:
  - ja

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor
